language: python
python:
  - "3.6"

install:
  - pip install -r requirements.txt
  - pip install coveralls

script:
  - pytest -v --cov api/app --cov-report term-missing
  - nosetests -v --with-coverage --api
 
after_success:

  - coveralls
